--- a/src/app/api/login/route.ts
+++ b/src/app/api/login/route.ts
@@ -2,11 +2,12 @@
 import { createSessionToken } from '@/lib/auth';
 
 export async function POST(request: NextRequest) {
-  const configured = process.env.APP_ACCESS_CODE && process.env.APP_SESSION_SECRET;
-  if (!configured) return NextResponse.json({ error: 'App access is not configured.' }, { status: 503 });
+  const accessCode = process.env.APP_ACCESS_CODE;
+  const sessionSecret = process.env.APP_SESSION_SECRET;
+  if (!accessCode || !sessionSecret) return NextResponse.json({ error: 'App access is not configured.' }, { status: 503 });
   const body = await request.json().catch(() => ({}));
-  if (body.accessCode !== process.env.APP_ACCESS_CODE) return NextResponse.json({ error: 'Invalid access code.' }, { status: 401 });
+  if (body.accessCode !== accessCode) return NextResponse.json({ error: 'Invalid access code.' }, { status: 401 });
   const response = NextResponse.json({ ok: true });
-  response.cookies.set('jd_session', await createSessionToken(process.env.APP_SESSION_SECRET), { httpOnly: true, sameSite: 'lax', secure: process.env.NODE_ENV === 'production', path: '/', maxAge: 30 * 24 * 60 * 60 });
+  response.cookies.set('jd_session', await createSessionToken(sessionSecret), { httpOnly: true, sameSite: 'lax', secure: process.env.NODE_ENV === 'production', path: '/', maxAge: 30 * 24 * 60 * 60 });
   return response;
 }
--- a/src/lib/job-sources/index.ts
+++ b/src/lib/job-sources/index.ts
@@ -42,6 +42,7 @@
   const arrangement = input.workArrangement ?? inferArrangement(input.location, input.description);
   const fp = input.duplicateFingerprint ?? fingerprint([input.company, input.title, input.location, input.externalId, input.applicationUrl]);
   return {
+    ...input,
     id: input.id ?? nanoid(),
     title: input.title.trim(),
     company: input.company.trim(),
@@ -58,7 +59,6 @@
     repost: input.repost ?? false,
     duplicateFingerprint: fp,
     active: input.active ?? true,
-    ...input,
   };
 }
 
--- a/src/lib/package-service.ts
+++ b/src/lib/package-service.ts
@@ -1,6 +1,6 @@
 import 'server-only';
 import { nanoid } from 'nanoid';
-import type { Analysis, ApplicationPackage, Job } from './types';
+import type { Analysis, ApplicationPackage, CoverLetterContent, Job } from './types';
 import { getState, updateState } from './storage/state';
 import { generateCoverLetter, tailorResume } from './ai';
 import { getMasterResumeText } from './career';
@@ -21,7 +21,7 @@
   return clamp(analysis.atsScore * 0.45 + keywordScore * 0.4 + 15);
 }
 
-function qa(job: Job, resume: { candidateName?: string; summary?: string; sections?: unknown[]; excludedUnsupportedKeywords?: string[] }, cover: { optimizationScore?: number } & Record<string, unknown>, ats: number): string[] {
+function qa(job: Job, resume: { candidateName?: string; summary?: string; sections?: unknown[]; excludedUnsupportedKeywords?: string[] }, cover: CoverLetterContent, ats: number): string[] {
   const failures: string[] = [];
   const resumeBlob = JSON.stringify(resume).toLowerCase();
   const coverBlob = JSON.stringify(cover).toLowerCase();
